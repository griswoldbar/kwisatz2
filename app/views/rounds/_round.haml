%li.span9
  .thumbnail.round{id: "round-#{round.id}" }
    .round_data
      =render 'rounds/round_data', round: round
    
:javascript
  $( ".round" ).draggable({ 
    revert: "invalid",
    helper: "clone"
  });